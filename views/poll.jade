extends layout

block content
	div.container
		h1= question
		form(action="/", method="post")
			each choice, i in choices
				div.radio
					label
						input(type="radio", name="choicesRadios", value="#{i}")
						=choice
			button(type="submit", class="btn btn-default") Submit

	